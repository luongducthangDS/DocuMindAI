# Copy to .env.local (Vite) hoặc set trong Vercel Project Settings → Environment Variables.
# Không set -> dùng "/api/v1" (dev local qua Vite proxy, hoặc same-origin).
# Set khi frontend (Vercel) và backend (Render) ở 2 domain khác nhau, vd:
VITE_API_URL=https://documind-ai-backend.onrender.com
