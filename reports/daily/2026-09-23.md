# Report 2026-09-23

> Ghi dần trong ngày, mỗi bước xong là thêm một mục. Giờ chỉ ghi khi có log thật.

## Đưa corpus lên Chroma Cloud (21:35)

- Copy collection `documind_legal` từ Chroma local lên Chroma Cloud (database `DocuMind`):
  **1151/1151 chunk**, dùng lại embeddings có sẵn, không gọi Gemini. Kiểm tra: dùng embedding của
  chunk đầu làm query, top-1 trả về đúng chunk đó (distance 0.0), metadata đủ trường.
- `chromadb.CloudClient` (0.6.3) lỗi `KeyError '_type'` với server Cloud 1.x → chuyển sang gọi
  REST v2. Script: `scripts/copy_chroma_to_cloud.py`; 3 biến `CHROMA_CLOUD_*` trong `.env` + `src/config.py`.
- Chroma Cloud chỉ lưu chunk (text + embedding + metadata), **không lưu file gốc**.
- ⚠️ API key Chroma bị dán vào chat → Ted cần rotate key rồi sửa `.env`. **Chưa làm.**

## Giải thích "sample chỉ thấy 18/VBHN-VPQH"

- Đếm trên cloud: 1151 chunk / **19 `so_hieu`**, đủ corpus. `18/VBHN-VPQH` là bản hợp nhất của
  BLLĐ 45/2019: chunk mang `so_hieu = 45/2019/QH14`, còn `consolidated_from = 18/VBHN-VPQH`
  (226/227 chunk). Mấy dòng đầu trên dashboard đều là chunk BLLĐ nên cột đó lặp lại.

## Kiểm tra truy hồi theo thời điểm → phát hiện corpus thiếu văn bản cũ

- Cơ chế `as_of_date` chọn đúng văn bản ở các giai đoạn có trong corpus (lương tối thiểu
  2025-03 → NĐ 74/2024, 2026-03 → NĐ 293/2025; trợ cấp thất nghiệp 2024 → Luật 38/2013, 2026 → 74/2025).
- **Hai chỗ hổng:** lương tối thiểu tại 2023-01 (thiếu NĐ 38/2022, 90/2019) và làm thêm giờ tại
  2019-06 (thiếu BLLĐ 10/2012, NĐ 45/2013, 05/2015) → truy hồi lạc đề.
- Đề xuất nạp theo nhóm: (1) chuỗi lương tối thiểu, (2) BLLĐ 2012, (3) văn bản sửa đổi cấp khoản.
  **Chờ Ted chọn, chưa làm.**

## Benchmark Qdrant Cloud vs Chroma Cloud (22:02 → 22:18)

- Viết `eval/provider_bench.py` (đo riêng tầng vector store), `src/rag/chroma_cloud.py` (REST dùng
  chung), `tests/test_provider_bench.py` (3 test). Full suite: **288/288 pass**.
- Kết quả (199 câu × 3 lần lặp, top-10, có filter, đo từ máy dev ở VN):

  | | Qdrant Cloud | Chroma Cloud |
  |---|---:|---:|
  | ANN recall@10 so với exact | 1.000 | 0.998 |
  | Recall@1 / @5 · MRR | 0.667 / 0.962 · 0.794 | y hệt |
  | Vi phạm filter | 0% | 0% |
  | Latency p50 / p95 | **209 / 324 ms** | 309 / 1336 ms |
  | RTT mạng p50 / p95 | 193 / 197 ms | 260 / 734 ms |

- Kết luận: chất lượng truy hồi như nhau, Qdrant nhanh hơn, chênh lệch chủ yếu do mạng (Qdrant ở
  Sydney, Chroma Cloud dải IP AWS). **Giữ Qdrant cho production** — Ted đồng ý ("hơn ở tốc độ là quá ok").
- Chi tiết: [`reports/provider_bench.md`](../provider_bench.md) · số thô `reports/provider_bench.json` ·
  MLflow `documind-provider-bench` / `provider-bench-1151`. README mục Đánh giá đã cập nhật.
- Chưa đo từ region của Render. Số này chưa đại diện cho latency production.

## Quy ước mới

- Ted yêu cầu có report theo ngày, **làm đến đâu ghi đến đó** → file này (`reports/daily/YYYY-MM-DD.md`).

## Còn dở

- [ ] Rotate API key Chroma Cloud (Ted).
- [ ] Nạp văn bản cũ — **Ted đã chọn (22:50): nhóm 1 chuỗi lương tối thiểu (NĐ 90/2019, 38/2022) + nhóm 2 BLLĐ 2012
  (10/2012/QH13, NĐ 45/2013, 05/2015)**; bỏ nhóm 3. Tiếp: ingest → backfill → eval → migrate Qdrant + copy Chroma Cloud.
- [x] `ci.yml` (3.10 → 3.11) khớp `.venv` 3.11.9 + Dockerfile `python:3.11-slim` → giữ. `.venv310-old/` = venv 3.10 cũ
  (19/09, 2.4GB) → ignore bằng `.venv*/`, chưa xoá (Ted tự xoá nếu không cần).
- [x] Commit (22:45), full suite **292/292 pass**, `npm run build` OK, quét secret sạch: `7025acb` fix Gemini
  timeout/cooldown + BM25 fallback + Qdrant payload index · `50aa42e` UI · `999a871` Chroma Cloud + bench ·
  `86a0390` CI/gitignore. Đã push `origin/main` (Ted duyệt).

## UI chat (23:xx)

- Ted: "UI quá tệ". Chụp 5 màn (landing/chat × desktop/mobile, dark) → sửa:
  - Câu trả lời bỏ card viền, đọc như văn bản full cột 780px; chữ 15px, gạch đầu dòng thụt lề treo.
  - Nguồn trích dẫn: từ lưới ô to → mỗi nguồn 1 dòng gọn. Nút Sao chép/Lưu + badge model/latency gộp 1 hàng.
  - Mobile: bỏ header-bar trùng topbar (topbar hiện tên tab), landing chỉ hiện 4 gợi ý.
  - Bug: reload giữa lúc stream → "••• 0.0s" treo mãi. `loadStoredMessages` giờ bỏ placeholder rỗng, phần đã stream đánh dấu "Đã dừng".
- `npm run build` (tsc + vite) OK. Chưa commit.

## Thiết kế UI (canvas Design)

- Canvas đề xuất 4 màn: trang chào, hội thoại + cột nguồn, điện thoại, danh sách 20 văn bản —
  https://claude.ai/artifact/EgcWDmuhitS3ZiY6JXyru8 . Số liệu lấy từ corpus thật (293/2025 Điều 3, 74/2024).
  Đề xuất mới: chip "Áp dụng tại" (as_of_date) trên UI, dòng thời gian mức lương. Chưa đụng code frontend.
- Đưa canvas vào code (`frontend/src/main.tsx`, `styles.css`, `index.html`): chip "Áp dụng tại" gửi `as_of_date`
  qua cả REST lẫn WS (backend đã hỗ trợ sẵn), cột "Nguồn trích dẫn" bên phải từ ≥1200px (bấm [n] hoặc "N nguồn"
  để đổi câu trả lời), câu trả lời lại thành thẻ, font Newsreader thay Lora, `--text3` đậm hơn cho đủ tương phản,
  tab Văn bản thành bảng. Verify thật: đặt 01/10/2025 hỏi lương tối thiểu vùng I → 4.960.000đ (NĐ 74/2024).
  `npm run build` OK. Chưa làm: nhãn còn/hết hiệu lực + dòng thời gian ở cột nguồn (API chưa trả trạng thái),
  bảng 20 văn bản corpus (`/documents` chỉ liệt kê file upload). Chưa commit.

## Câu trả lời "5 nguồn" khi Gemini hết quota

- Ted báo câu trả lời dán 5 nguồn thô, có cả biểu mẫu (Mẫu số 23) và điều khoản lạc đề (BLLĐ Điều 85 "rút khỏi thỏa ước").
  Gốc: cả 3 key × 2 model (`gemini-3.5-flash`, `gemini-3.1-flash-lite`) dính 429 → rơi về `_build_extractive_answer`,
  hàm này dán nguyên top-5, không lọc (reranker tắt nên `min_score=0`). Embedding cũng 429 → dense bị bỏ, chỉ còn BM25
  (lý do "rút BHXH" khớp "rút khỏi thỏa ước"); contextualize cũng chết nên "hưởng nhiều lần thì sao" mất ngữ cảnh.
- Sửa `src/rag/generator.py`: fallback bỏ chunk không có header "Điều" (biểu mẫu/phụ lục), tối đa 3 nguồn, giữ số [i]
  gốc để `_cited_sources` không gắn nhầm nguồn. Test mới `test_extractive_fallback_skips_forms_and_caps_sources`.
  `pytest` 293/293 pass. Chưa verify trên UI (Gemini vẫn đang hết quota). Chưa commit.
- Chưa sửa: xếp hạng BM25-only vẫn kém (câu "rút BHXH" đặt Điều 70 ở hạng 5) — cần quota Gemini/reranker mới cải thiện.
