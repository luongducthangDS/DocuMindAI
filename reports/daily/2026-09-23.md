# Report 2026-09-23

> Ghi dần trong ngày, mỗi bước xong là thêm một mục. Giờ chỉ ghi khi có log thật.

## Đưa corpus lên Chroma Cloud (21:35)

- Copy collection `documind_legal` từ Chroma local lên Chroma Cloud (database `DocuMind`):
  **1151/1151 chunk**, dùng lại embeddings có sẵn, không gọi Gemini. Kiểm tra: dùng embedding của
  chunk đầu làm query, top-1 trả về đúng chunk đó (distance 0.0), metadata đủ trường.
- `chromadb.CloudClient` (0.6.3) lỗi `KeyError '_type'` với server Cloud 1.x → chuyển sang gọi
  REST v2. Script: `scripts/copy_chroma_to_cloud.py`; 3 biến `CHROMA_CLOUD_*` trong `.env` + `src/config.py`.
- Chroma Cloud chỉ lưu chunk (text + embedding + metadata), **không lưu file gốc**.
- ⚠️ API key Chroma bị dán vào chat → Ted cần rotate key rồi sửa `.env`. **Chưa làm.**

## Giải thích "sample chỉ thấy 18/VBHN-VPQH"

- Đếm trên cloud: 1151 chunk / **19 `so_hieu`**, đủ corpus. `18/VBHN-VPQH` là bản hợp nhất của
  BLLĐ 45/2019: chunk mang `so_hieu = 45/2019/QH14`, còn `consolidated_from = 18/VBHN-VPQH`
  (226/227 chunk). Mấy dòng đầu trên dashboard đều là chunk BLLĐ nên cột đó lặp lại.

## Kiểm tra truy hồi theo thời điểm → phát hiện corpus thiếu văn bản cũ

- Cơ chế `as_of_date` chọn đúng văn bản ở các giai đoạn có trong corpus (lương tối thiểu
  2025-03 → NĐ 74/2024, 2026-03 → NĐ 293/2025; trợ cấp thất nghiệp 2024 → Luật 38/2013, 2026 → 74/2025).
- **Hai chỗ hổng:** lương tối thiểu tại 2023-01 (thiếu NĐ 38/2022, 90/2019) và làm thêm giờ tại
  2019-06 (thiếu BLLĐ 10/2012, NĐ 45/2013, 05/2015) → truy hồi lạc đề.
- Đề xuất nạp theo nhóm: (1) chuỗi lương tối thiểu, (2) BLLĐ 2012, (3) văn bản sửa đổi cấp khoản.
  **Chờ Ted chọn, chưa làm.**

## Benchmark Qdrant Cloud vs Chroma Cloud (22:02 → 22:18)

- Viết `eval/provider_bench.py` (đo riêng tầng vector store), `src/rag/chroma_cloud.py` (REST dùng
  chung), `tests/test_provider_bench.py` (3 test). Full suite: **288/288 pass**.
- Kết quả (199 câu × 3 lần lặp, top-10, có filter, đo từ máy dev ở VN):

  | | Qdrant Cloud | Chroma Cloud |
  |---|---:|---:|
  | ANN recall@10 so với exact | 1.000 | 0.998 |
  | Recall@1 / @5 · MRR | 0.667 / 0.962 · 0.794 | y hệt |
  | Vi phạm filter | 0% | 0% |
  | Latency p50 / p95 | **209 / 324 ms** | 309 / 1336 ms |
  | RTT mạng p50 / p95 | 193 / 197 ms | 260 / 734 ms |

- Kết luận: chất lượng truy hồi như nhau, Qdrant nhanh hơn, chênh lệch chủ yếu do mạng (Qdrant ở
  Sydney, Chroma Cloud dải IP AWS). **Giữ Qdrant cho production** — Ted đồng ý ("hơn ở tốc độ là quá ok").
- Chi tiết: [`reports/provider_bench.md`](../provider_bench.md) · số thô `reports/provider_bench.json` ·
  MLflow `documind-provider-bench` / `provider-bench-1151`. README mục Đánh giá đã cập nhật.
- Chưa đo từ region của Render. Số này chưa đại diện cho latency production.

## Quy ước mới

- Ted yêu cầu có report theo ngày, **làm đến đâu ghi đến đó** → file này (`reports/daily/YYYY-MM-DD.md`).

## Còn dở

- [ ] Rotate API key Chroma Cloud (Ted).
- [ ] Chọn nhóm văn bản cũ để nạp (Ted) → ingest, backfill, eval, đồng bộ Qdrant + Chroma Cloud.
- [ ] `.github/workflows/ci.yml` và `.venv310-old/` có thay đổi **không phải do Claude** → Ted kiểm tra trước khi commit.
- [ ] Chưa commit các thay đổi hôm nay.
