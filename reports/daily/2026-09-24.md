# Report 2026-09-24

> Ghi dần trong ngày, mỗi bước xong là thêm một mục. Giờ chỉ ghi khi có log thật.

## Chẩn đoán Render chậm (log qua Render CLI)

- Render CLI v2.28 đọc được log runtime của service `srv-damka6cri2ms73dn281g` (region oregon, plan free).
- Đo 1 câu thật `POST /query` ("Thời gian thử việc tối đa là bao lâu?"): **49s**. Theo từng bước:
  phân loại 2.0s, tìm kiếm 1.7s, grade 0.8s, **tổng hợp 44s** → Gemini trả
  `503 This model is currently experiencing high demand` cho mọi cặp (key, model) → trích nguyên văn.
  Log 2026-09-23 có query 140–865s (504 Deadline, 429 quota).
- Cold start: service tắt sau ~15 phút không có ai dùng (7 lần shutdown từ 23/09). App tự khởi động mất
  30s (10:08:39 → 10:09:09: import 9s, embed probe 11s, nạp BM25 5s, retriever 5s),
  cộng thêm thời gian Render spin-up container.
- `/health` bị Render health check gọi mỗi ~5s, mỗi lần 0.7–1.9s: tạo `QdrantClient` mới + gọi
  Qdrant Sydney từ Oregon, là call đồng bộ trong `async def` → chặn event loop. Có lúc trả 429.
- Vercel không phải nguyên nhân: tải trang chính 0.8s, frontend chỉ gọi `/health` 1 lần khi mount.
- 00:06–00:18 UTC có `POST /query → 400` đều đặn mỗi 60s — chưa rõ nguồn.

## Đếm lời gọi LLM mỗi query (Langfuse)

- Org Langfuse (tạo sau 16/09/2026) chỉ đọc được bằng API `GET /api/public/v2/observations`; `/api/public/traces` trả `LEGACY_API_UNAVAILABLE`.
- Trace câu thử `perf-probe-1` có 2 generation (phân loại + grade). Bước tổng hợp mất 44s và thử Gemini nhiều lần
  nhưng **không có span nào**, vì `record_generation` chỉ được gọi khi Gemini trả lời thành công (generator.py:343, :595; graph.py:204, :261).
- Trace WS từ UI lúc 10:19:55 có 0 generation: mọi cặp (key, model) còn đang cooldown 120s sau lỗi 503 → trích nguyên văn ngay.

## UI hiện đại + tiến trình xử lý (theo Data Analysis Agent)

- Ted: UI "cổ điển quá", muốn hiện đại, có tiến trình các bước đẹp như dự án Data agent.
- Backend `src/api/routes/query.py`: WS phát `{"step": {label, detail, ms}}` sau mỗi bước (Phân tích câu hỏi →
  Tìm kiếm tài liệu → Lọc theo hiệu lực → Tổng hợp câu trả lời), cùng dạng ThinkingStep của REST. Opt-in qua
  `"progress": true` — client cũ không nhận JSON lạ. Test mới `tests/test_ws_progress.py`; full suite **295/295 pass**.
- Frontend: `AgentTrace` (4 giai đoạn ngang, vòng xoay + đồng hồ bước đang chạy, nhật ký chi tiết có ms từng bước,
  thu gọn khi xong) thay `ThinkingPanel` emoji; skeleton thay 3 chấm. Theme "cosmic" như Data agent (tối mặc định,
  xanh ngọc + tím, Space Grotesk + IBM Plex Mono), light theme cùng họ màu. `npm run build` OK.
- Verify thật (backend local): "Thời gian thử việc tối đa…" → tiến trình chạy từng bước, 4/4 giai đoạn · 6.6s;
  xem cả mobile (nhãn ngắn) và light theme. Đường REST chỉ có bước lúc xong → lúc chờ chỉ hiện giai đoạn 1 đang chạy.
- Chưa commit/push.
- Ted: "2 dự án same same" → đổi DocuMind sang phong cách cổng thông tin hành chính: nền giấy sáng (mặc định),
  đỏ đậm #a4161a + vàng kim #c9a227, dải tiêu đề đỏ viền vàng (cả mobile topbar), Noto Serif cho tiêu đề +
  Be Vietnam Pro, góc 6px, bỏ glow/gradient. Tiến trình thành "Bước 1–4" (nút tròn đánh số, bước đang chạy vòng
  vàng kim). Không dùng quốc huy/tên cơ quan thật. Build OK; xem thật desktop + mobile ("Làm thêm giờ…" 4/4 · 14.9s).
